{
  "name": "agents-n-arbiters",
  "version": "1.0.0",
  "description": "",
  "author": "Guy Royse <guy@guy.dev>",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@azure/static-web-apps-cli": "^2.0.6",
    "azure-functions-core-tools": "^4.2.2",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "dev": "npm-run-all --sequential build --parallel dev:web",
    "dev:web": "npm run dev --workspace=ana-web",
    "build": "npm-run-all --sequential build:shared --parallel build:web build:api",
    "build:shared": "npm run build --workspace=@ana/shared",
    "build:web": "npm run build --workspace=ana-web",
    "build:api": "npm run build --workspace=ana-api"
  }
}
